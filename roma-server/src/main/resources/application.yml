spring:
  profiles:
    active: dev
    include: privilege
mybatis:
  mapper-locations: classpath:mapper/*/*.xml

logging:
  config: classpath:config/logback-spring.xml
  name: roma

roma:
  security:
    logout: /api/base/login/logout


